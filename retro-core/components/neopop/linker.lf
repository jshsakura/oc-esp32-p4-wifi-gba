# NeoPop's state goes to PSRAM.
#
# This core was a standalone app until it was folded into retro-core, and folding
# trades one ceiling for another: the flash saving is a whole ~950KB framework
# copy and a partition, but every static the core carries now lands in the same
# internal RAM as the systems already here. bios.c's HLE BIOS image (64KB),
# mem.c's work RAM (48KB) and gfx.c's framebuffer (48KB) are 170KB between them,
# and adding them was enough to fail the link with esp-idf's "discards section",
# which is how it reports running out of internal RAM (BRINGUP 0.3-2).
#
# The scheme is defined here rather than reusing esp-idf's extram_bss: that one
# lives in esp_wifi's fragment, and this app builds with networking off, so the
# name resolves to nothing -- silently, leaving the link failing at exactly the
# same byte count as before.
# Do NOT add `text -> extern_ram` here. It links, it places 172KB of .text at
# 0x480xxxxx, and the board dies with an illegal instruction at the first call
# into this core -- a linker fragment assigns ADDRESSES, it does not arrange for
# anything to be loaded there, so the CPU executes uninitialised PSRAM. Getting
# code into PSRAM is a boot-time job (CONFIG_SPIRAM_XIP_FROM_PSRAM), not a
# placement one. Tried 2026-08-04 while pricing instruction fetch from PSRAM.
[scheme:neopop_extram]
entries:
    bss -> extern_ram
    common -> extern_ram
    legacy_bss -> extern_ram

[mapping:neopop_bss]
archive: libneopop.a
entries:
    * (neopop_extram)
