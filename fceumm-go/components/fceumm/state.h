/*
 * Stub placed at the component root for the relative include "../state.h"
 * used by src/input/share.h. The "../" resolves up one directory from each
 * entry in COMPONENT_ADD_INCLUDEDIRS; from "gw_compat" it lands here.
 * See gw_compat/state.h for the full explanation of the cart.h/state.h rename.
 */
#ifndef FCEUMM_ROOT_STATE_H
#define FCEUMM_ROOT_STATE_H
#include "fceu-state.h"
#endif
