__pycache__/
.DS_Store/
.cache/
.vscode/
build/

# ESP32 build
/partitions.bin
/partitions.csv
/sdkconfig.old
/sdkconfig
/*/sdkconfig.old
/*/sdkconfig
/*.fw
/*.zip
# Built images. The base repo's prebuilt retro-go_2.0_esp32p4.img is already tracked and
# stays that way -- it is the only working reference binary this fork shipped with.
/*.img
# Older images moved out of the root to keep it readable. Nothing here is needed to build.
/images/

# Agent worktrees live here, each a full checkout of this repo.
/.claude/

# SDL2 build
/sd
/stderr.txt
/stdout.txt
/gmon.out
/*.exe

# esp-idf component manager
managed_components/
dependencies.lock

sdkconfig.rgtarget
/*/sdkconfig.rgtarget
