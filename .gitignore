__pycache__/
.DS_Store/
.cache/
.vscode/
build/

# ESP32 build
/partitions.bin
/partitions.csv
/sdkconfig.old
/sdkconfig
/*/sdkconfig.old
/*/sdkconfig
/*.fw
/*.zip
# Built images. The base repo's prebuilt retro-go_2.0_esp32p4.img is already tracked and
# stays that way -- it is the only working reference binary this fork shipped with.
/*.img

# SDL2 build
/sd
/stderr.txt
/stdout.txt
/gmon.out
/*.exe

# esp-idf component manager
managed_components/
dependencies.lock
