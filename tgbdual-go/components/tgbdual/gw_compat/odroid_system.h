#ifndef ODROID_SYSTEM_H
#define ODROID_SYSTEM_H
/* Empty stub. The G&W fork of tgbdual-go includes <odroid_system.h> in
 * tgbdual_mbc.cpp but never calls anything from it (all debug printfs are
 * commented out). On ESP32 we just need the include to resolve. */
#endif
